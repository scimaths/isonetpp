inherit:
  - ../module_configs/encoder.yaml
  - ../module_configs/prop_layer.yaml
  - ../module_configs/aggregator.yaml
  - ../module_configs/ntn_node.yaml

model_config:
  propagation_layer_config:
    prop_type: embedding

  propagation_steps: 5
  # scoring params
  scoring: ntn
  # interaction params
  interaction_when: never

name: gmn_baseline_scoring=ntn___tp=none