inherit:
  - ../module_configs/encoder.yaml
  - ../module_configs/prop_layer.yaml
  - ../module_configs/aggregator.yaml

model_config:
  propagation_layer_config:
    prop_type: embedding

  propagation_steps: 5
  # scoring params
  scoring: aggregated
  # interaction params
  interaction_when: never

name: gmn_baseline_scoring=agg___tp=none