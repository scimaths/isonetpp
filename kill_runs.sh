pkill -f "subgraph.*${1}.*DATASET_NAME=aids.*SEED=0"
pkill -f "subgraph.*${1}.*DATASET_NAME=aids.*SEED=1"
pkill -f "subgraph.*${1}.*DATASET_NAME=aids.*SEED=3"
pkill -f "subgraph.*${1}.*DATASET_NAME=aids.*SEED=4"
pkill -f "subgraph.*${1}.*DATASET_NAME=mutag.*SEED=0"
pkill -f "subgraph.*${1}.*DATASET_NAME=mutag.*SEED=1"
pkill -f "subgraph.*${1}.*DATASET_NAME=mutag.*SEED=3"
pkill -f "subgraph.*${1}.*DATASET_NAME=mutag.*SEED=4"
pkill -f "subgraph.*${1}.*DATASET_NAME=ptc_fm.*SEED=0"
pkill -f "subgraph.*${1}.*DATASET_NAME=ptc_fm.*SEED=1"
pkill -f "subgraph.*${1}.*DATASET_NAME=ptc_fm.*SEED=2"
pkill -f "subgraph.*${1}.*DATASET_NAME=ptc_fm.*SEED=3"
pkill -f "subgraph.*${1}.*DATASET_NAME=ptc_fr.*SEED=0"
pkill -f "subgraph.*${1}.*DATASET_NAME=ptc_fr.*SEED=2"
pkill -f "subgraph.*${1}.*DATASET_NAME=ptc_fr.*SEED=3"
pkill -f "subgraph.*${1}.*DATASET_NAME=ptc_fr.*SEED=4"
pkill -f "subgraph.*${1}.*DATASET_NAME=ptc_mm.*SEED=0"
pkill -f "subgraph.*${1}.*DATASET_NAME=ptc_mm.*SEED=1"
pkill -f "subgraph.*${1}.*DATASET_NAME=ptc_mm.*SEED=2"
pkill -f "subgraph.*${1}.*DATASET_NAME=ptc_mm.*SEED=4"
pkill -f "subgraph.*${1}.*DATASET_NAME=ptc_mr.*SEED=0"
pkill -f "subgraph.*${1}.*DATASET_NAME=ptc_mr.*SEED=2"
pkill -f "subgraph.*${1}.*DATASET_NAME=ptc_mr.*SEED=3"
pkill -f "subgraph.*${1}.*DATASET_NAME=ptc_mr.*SEED=4"
